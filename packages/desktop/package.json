{
  "name": "@dsrd/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "concurrently -k -s first \"npm run dev --workspace=@dsrd/web -- --host 127.0.0.1 --strictPort\" \"wait-on http-get://127.0.0.1:5173 && cross-env DSRD_RENDERER_URL=http://127.0.0.1:5173 electron dist/main.js\""
  },
  "dependencies": {
    "@dsrd/api": "0.1.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "cross-env": "^10.1.0",
    "electron": "^44.0.0",
    "wait-on": "^9.1.0"
  }
}
