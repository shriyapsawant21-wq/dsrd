# C5 — Minimized Artifact and Replay

## Fresh-session protocol
Start a fresh Codex session for this checkpoint. Use Superpowers before taking any action. Read AGENTS.md, docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md, docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md, docs/checkpoints.md, and this prompt. Work only within the ownership stated below. After running the verification command, update docs/checkpoints.md with evidence, remaining work, dependencies, and blockers.
## Timeline position
**Order: 4 — team integration.** Start only after C4 proves a real discovered failure. Akil drives minimization/artifact, Riya drives physical replay, and Shriya verifies reproduced evidence. When complete, update the board with: `C5 implemented: minimized artifact replay verified. Moving to C6.`

## Goal
Save a generic artifact and deterministically replay its minimized failure through the same adapter/oracle path.

## Non-goals
Do not add a duplicate replay implementation in scheduler.

## Owner
Akil, Riya, Shriya jointly.

## Inputs
Completed C4 failure discovery and a target-aware artifact.

## Requested work
Minimize the discovered schedule, serialize target and evidence, invoke adapter replay, and verify the oracle reproduces expected failure.

## Acceptance criteria
The minimized perturbations are no larger than the original; replay produces expected failure and timeline evidence.

## Verification
npm test && npm run build && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C6 as next checkpoint in docs/checkpoints.md.
