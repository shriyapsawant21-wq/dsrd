# C2 — Compose Execution Platform

## Goal
Expose Compose through the generic ExecutionPlatform interface.

## Non-goals
Do not add database-specific behavior or move failure classification into runtime.

## Owner
Riya.

## Inputs
Merged C0 contract v2 and existing Compose controller.

## Requested work
Discover Compose services as workloads; advertise supported phases; translate generic perturbations; preserve reset, observation, and replay paths.

## Acceptance criteria
Scheduler can execute and replay a generic schedule without Docker concepts crossing its boundary.

## Verification
npm test -- packages/runtime && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C4 as next checkpoint in docs/checkpoints.md.

