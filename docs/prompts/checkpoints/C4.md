# C4 — Compose Discovery

## Fresh-session protocol
Start a fresh Codex session for this checkpoint. Use Superpowers before taking any action. Read AGENTS.md, docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md, docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md, docs/checkpoints.md, and this prompt. Work only within the ownership stated below. After running the verification command, update docs/checkpoints.md with evidence, remaining work, dependencies, and blockers.
## Timeline position
**Order: 3 — team integration.** Start only when C1, C2, and C3 are all marked implemented and their branches are integrated. Akil drives search, Riya drives execution/reset, and Shriya verifies proof evidence. When complete, update the board with: `C4 implemented: normal pass and generic Compose failure discovery verified. Moving to C5.`

## Goal
Prove normal pass followed by automatic discovery of a real generic workload timing failure.

## Non-goals
Do not add Kubernetes or database-specific scheduler code.

## Owner
Akil, Riya, Shriya jointly.

## Inputs
Completed C1 generic scheduler, C2 Compose adapter, and C3 proof layer.

## Requested work
Run the fixture baseline, explore discovered workload perturbations, and capture the first oracle-classified failure.

## Acceptance criteria
Baseline passes; at least one explored schedule fails; the failure includes normalized timeline evidence.

## Verification
npm test && npm run build && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C5 as next checkpoint in docs/checkpoints.md.
