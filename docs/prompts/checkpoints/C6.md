# C6 — Local-Process Adapter

## Goal
Add manifest-defined local processes, jobs, and initializers as a second execution platform.

## Non-goals
Do not add a database adapter or change generic scheduler semantics.

## Owner
Riya owns adapter lifecycle; Shriya owns local proof fixture; Akil owns target/CLI wiring.

## Inputs
Stable C4 and C5 generic Compose pipeline.

## Requested work
Define a local manifest, discover workloads, control process lifecycle, normalize observations, and prove the complete generic pipeline.

## Acceptance criteria
A local fixture demonstrates baseline pass, failure discovery, minimization, artifact, and replay.

## Verification
npm test && npm run build && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C7 as next checkpoint in docs/checkpoints.md.

