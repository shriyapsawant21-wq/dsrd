# C6 — Local-Process Adapter

## Fresh-session protocol
Start a fresh Codex session for this checkpoint. Use Superpowers before taking any action. Read AGENTS.md, docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md, docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md, docs/checkpoints.md, and this prompt. Work only within the ownership stated below. After running the verification command, update docs/checkpoints.md with evidence, remaining work, dependencies, and blockers.
## Timeline position
**Order: 5 — second platform.** Start only after C4 and C5 are stable. Riya starts local-process lifecycle work, Shriya builds proof fixture support, and Akil adds target/CLI wiring when their interfaces are ready. When complete, update the board with: `C6 implemented: local-process pipeline verified. Moving to C7 admission review.`

## Goal
Add manifest-defined local processes, jobs, and initializers as a second execution platform.

## Non-goals
Do not add a database adapter or change generic scheduler semantics.

## Owner
Riya owns adapter lifecycle; Shriya owns local proof fixture; Akil owns target/CLI wiring.

## Inputs
Stable C4 and C5 generic Compose pipeline.

## Requested work
Define a local manifest, discover workloads, control process lifecycle, normalize observations, and prove the complete generic pipeline.

## Acceptance criteria
A local fixture demonstrates baseline pass, failure discovery, minimization, artifact, and replay.

## Verification
npm test && npm run build && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C7 as next checkpoint in docs/checkpoints.md.
