# C7 — Kubernetes Adapter Admission

## Fresh-session protocol
Start a fresh Codex session for this checkpoint. Use Superpowers before taking any action. Read AGENTS.md, docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md, docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md, docs/checkpoints.md, and this prompt. Work only within the ownership stated below. After running the verification command, update docs/checkpoints.md with evidence, remaining work, dependencies, and blockers.

## Goal
Define the conditions for a future Kubernetes adapter without implementing it in the MVP.

## Non-goals
Do not install cluster tooling, add Kubernetes dependencies, or require a cluster for normal development.

## Owner
Riya owns lifecycle adapter; Shriya owns probe/evidence support; Akil owns generic scheduler integration.

## Inputs
Repeatedly passing C4 and C5 plus a disposable Kubernetes cluster.

## Requested work
When admitted, discover Deployments, StatefulSets, and Jobs as workloads; reset namespace-scoped resources; and replay using shared oracle semantics.

## Acceptance criteria
C7 stays planned until C4/C5 are stable and the team explicitly starts it.

## Verification
rg -n "Status: planned|Kubernetes" docs/checkpoints.md docs/prompts/checkpoints/C7.md

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and no next checkpoint in docs/checkpoints.md.
