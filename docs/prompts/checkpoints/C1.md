# C1 — Generic Scheduler

## Fresh-session protocol
Start a fresh Codex session for this checkpoint. Use Superpowers before taking any action. Read AGENTS.md, docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md, docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md, docs/checkpoints.md, and this prompt. Work only within the ownership stated below. After running the verification command, update docs/checkpoints.md with evidence, remaining work, dependencies, and blockers.
## Timeline position
**Order: 2 — parallel phase.** Akil starts C1 after C0 is staged, at the same time as Riya starts C2 and Shriya starts C3. C1 does not wait for C2/C3. When complete, update the board with: `C1 implemented: generic scheduler verified. Moving to C4; waiting for C2 and C3.`

## Goal
Migrate Akil's scheduler to generic workload/phase perturbations and a fake ExecutionPlatform.

## Non-goals
Do not execute Docker, local processes, or classify failures from logs.

## Owner
Akil.

## Inputs
Create a dedicated branch/worktree from `f8be82c` (`feat/service-agnostic`) before changing code. C0 is staged, not merged; do not reintroduce v1 types.

## Requested work
Generate schedules from discovered workload capabilities; preserve deterministic search/minimization; persist target-aware artifacts; replay through an injected adapter function.

## Acceptance criteria
No database names are built into scheduler behavior; fake-platform tests cover search, minimization, artifact, and replay.

## Verification
npm test -- packages/scheduler && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C4 as next checkpoint in docs/checkpoints.md.
