# C3 — Workload Proof Evidence

## Fresh-session protocol
Start a fresh Codex session for this checkpoint. Use Superpowers before taking any action. Read AGENTS.md, docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md, docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md, docs/checkpoints.md, and this prompt. Work only within the ownership stated below. After running the verification command, update docs/checkpoints.md with evidence, remaining work, dependencies, and blockers.

## Goal
Normalize proof input and timeline evidence around workload IDs while preserving oracle ownership.

## Non-goals
Do not make scheduler decisions from logs or classify adapter/reset errors as races.

## Owner
Shriya.

## Inputs
Merged C0 contract v2 and normalized observations from adapters.

## Requested work
Accept workload-oriented observations, retain deterministic pass/fail evaluation, and emit timeline events suitable for generic replay evidence.

## Acceptance criteria
A process, job, initializer, or service can appear in proof/timeline output; RunResult.status remains oracle-produced.

## Verification
npm test -- packages/proof && npm run typecheck

## Checkpoint update
Record status, evidence, commit/PR, remaining work, dependencies, blockers, and C4 as next checkpoint in docs/checkpoints.md.
