# C3 — Workload Proof Evidence

## Mission

Make proof observations and timeline evidence workload-oriented while retaining deterministic oracle ownership of RunResult.status.

## Timeline and ownership

**Order:** 2, parallel. Start after C0 staging from the exact base commit; run beside C1 and C2 without waiting for them.
**Lead:** Shriya.
**Branch/worktree:** create `feat/workload-proof` from `f8be82c`. Do not work on `dev`, `main`, or another owner's branch.

## Fresh Codex execution protocol

You are an implementation agent, not an adviser. Start a fresh Codex session. Before acting, use Superpowers and read, in order: `AGENTS.md`, this prompt, `docs/superpowers/specs/2026-08-29-service-agnostic-debugger-design.md`, `docs/superpowers/plans/2026-08-29-service-agnostic-debugger.md`, `docs/contracts/shared-contracts.md`, and `docs/checkpoints.md`.

Use an isolated worktree. Follow the relevant Superpowers workflow: brainstorming/planning if scope changes, TDD for behavior changes, systematic debugging for failures, and verification-before-completion before any success claim or commit. Work autonomously through safe in-scope steps.

## Scope

**Change only:** `packages/proof/**` and the C3 record in `docs/checkpoints.md`.

**Do not:** Do not edit scheduler generation/minimization, runtime lifecycle, shared contracts, or classify execution/reset exceptions as discovered races.

## Required execution sequence

1. Inspect the current implementation and tests; state any contradiction with this prompt before changing code.
2. Write or update a focused failing test first for every behavior change; run it and confirm the expected failure.
3. Implement the smallest change that makes the focused test pass.
4. Run the checkpoint verification command below plus any focused tests needed for the changed behavior.
5. Run `git diff --check`, commit only owned files plus `docs/checkpoints.md`, and report the commit hash.
6. Never mark this checkpoint complete without fresh command output.

## Verification

```bash
npm test -- packages/proof && npm run typecheck --workspace @dsrd/proof
```

## Mandatory checkpoint handoff

After verification, update `docs/checkpoints.md` in the same commit:

```text
Status: complete
Evidence: Implemented: workload-normalized proof and timeline evidence verified with deterministic oracle behavior preserved; moving to C4 and waiting for C1/C2.
Commit/PR: <commit hash or PR>
Remaining work: none
Dependencies: <ready or exact checkpoint still awaited>
Blockers: none
Next checkpoint: <checkpoint>
```

If another owner's work blocks progress, do not edit their package or invent a replacement. Set `Status: blocked`, record the exact missing output and owner, commit that status update if appropriate, and stop.
